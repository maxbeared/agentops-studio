# Single stage for WebSocket server
FROM oven/bun:1-alpine

WORKDIR /app

COPY package.json bun.lock ./
COPY apps/ ./apps/
COPY packages/ ./packages/

RUN bun install

# WebSocket runs directly from source (bun can run TS)
EXPOSE 3002

ENTRYPOINT ["bun", "run", "apps/api/src/websocket-server.ts"]
